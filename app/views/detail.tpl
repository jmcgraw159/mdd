 <div class="detail">
 	<div class="row">
 		<article class="small-12 small-centered medium-12 medium-uncentered large-12 large-uncentered columns">
 			<h2>{{article.title}}</h2>
 			<img ng-src="{{article.url}}" alt="{{article.title}}">
 			<p>{{article.desc}}</p>
 		</article>
 	</div>
 </div>